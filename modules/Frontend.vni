package vnx.web;

import vnx.TopicPtr;

module Frontend {
	
	string address = "127.0.0.1:8080";
	
	TopicPtr input;
	
	TopicPtr output;
	
	TopicPtr channel;
	
	int listen_queue_size = 256;
	
	int connection_timeout_ms = 5000;
	
	long read_block_size = 4096;		// [bytes]
	
	long write_block_size = 4096;		// [bytes]
	
	
	void handle(StreamWrite sample);
	void handle(StreamEventArray sample);
	
}
