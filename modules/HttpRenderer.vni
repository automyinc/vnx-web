package vnx.web;

import vnx.TopicPtr;

module HttpRenderer {
	
	TopicPtr input;
	
	TopicPtr output;
	
	int interval_ms = 20;			// write interval for big files
	
	long chunk_size = 524288;		// chunk size for big files [bytes]
	
	
	void handle(HttpResponse sample);
	void handle(StreamEventArray sample);
	
}
