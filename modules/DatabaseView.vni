package vnx.web;

import vnx.TopicPtr;

module DatabaseView {
	
	string database = "Database";
	
	TopicPtr domain;
	
	TopicPtr input;
	
	Path path = "/database/";
	
	string password;				// needs to be at least 6 chars long
	
	
	void handle(Request sample);
	
}
