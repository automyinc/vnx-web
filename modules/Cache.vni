package vnx.web;

import vnx.TopicPtr;

module Cache {
	
	TopicPtr domain;
	
	TopicPtr channel;
	
	long num_entries = 1024;		// number of cache entries
	
	long max_memory;				// maximum memory [bytes]
	
	
	void purge();
	
	void handle(Request sample);
	void handle(Response sample);
	void handle(Provider sample);
	void handle(Content sample);
	
}
